# LLM (OpenAI SDK-compatible; DeepSeek in this setup)
LLM_API_KEY=sk-xxx
LLM_BASE_URL=https://api.deepseek.com
# Flash = fast non-thinking (default), Pro = thinking/reasoning
LLM_MODEL=deepseek-v4-flash
LLM_MODEL_PRO=deepseek-v4-pro

# Kimi — used only for image-captioning. moonshot-v1-8k-vision-preview is
# a non-thinking vision model: ~2s per caption. K2.6 is more capable but
# burns 30-45s on its reasoning chain (and ignores enable_thinking=false /
# reasoning_effort=low), which is too slow for our async caption job.
KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.cn/v1
KIMI_VISION_MODEL=moonshot-v1-8k-vision-preview

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_platform

# Redis
REDIS_URL=redis://localhost:6379

# Auth
AUTH_SECRET=change-me-in-production

# Agent runtime
AGENT_RUNTIME_URL=http://localhost:3001
MOCK_LLM=false

# Web search (used by web_search / search_lyrics tools).
# Primary tries first; fallback runs only on primary error.
WEB_SEARCH_PRIMARY=bocha
WEB_SEARCH_FALLBACK=tavily
BOCHA_API_KEY=
TAVILY_API_KEY=

# Tencent Cloud COS (image uploads)
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
TENCENT_COS_BUCKET=
TENCENT_COS_REGION=ap-shanghai
